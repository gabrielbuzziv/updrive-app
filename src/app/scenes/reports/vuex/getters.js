export default {
    /**
     * Report Status
     */
    'reports/GET_REPORTS' ({ reports }) {
        return reports
    },

    'reports/GET_PAGE' ({ page }) {
        return page
    },

    'reports/GET_RECORDS' ({ records }) {
        return records
    },
}